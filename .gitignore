# Data pipeline — local zones aren't tracked. The deployed backend
# only needs the Iceberg consumable slice + catalog + reference YAMLs;
# those are explicitly re-included below.
data/ai_ready/
data/bronze/
data/silver/
data/governance/
data/raw/
data/warehouse/
data/futureproof.duckdb
data/catalog.db
data/catalog/*.bak*
data/gold/catalog.db
data/gold/iceberg_catalog.db
data/gold/pyiceberg_catalog.db

# CLI harness local state
backend/data/
data/builds/

# Gemma call logs (JSONL, one record per API call)
logs/

# Video collateral — source video files and extracted frames are large
# and live alongside the README link to YouTube. Not tracked in git.
docs/video/
backend/.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
dist/
build/
.venv/
.env

# Python
.mypy_cache/
.ruff_cache/

# Node
node_modules/

# IDE
.vscode/
.idea/
*.swp

# TypeScript build artifacts
*.d.ts
*.d.ts.map
!src/**/*.d.ts

# OS
.DS_Store

# Midjourney source PNGs — derivatives live in frontend/public/campus/.
# Originals backed up at ~/Documents/Midjourney.
Midjourney/

# Build artifacts
*.tsbuildinfo

# Agent runtime state
.claude/*.lock

# Internal review notes — not for the competition-facing repo
reports/reviews/

# Kaggle writeup drafts — submission text is on Kaggle, not in the public repo
docs/kaggle/
