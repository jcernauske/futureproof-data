# CDE Registry: raw-ingest-anthropic-economic-index

**Spec:** `docs/specs/raw-ingest-anthropic-economic-index.md`
**Primary tagging document:** `governance/cde-tagging/raw-anthropic-economic-index.md` *(full rationale per column per zone lives there)*
**Date:** 2026-04-16
**Agent:** @cde-tagger
**Contract targets:**
- `governance/data-contracts/raw-anthropic-economic-index.yaml` (CREATE, Bronze) — to be generated by @doc-generator
- `governance/data-contracts/base-anthropic-observed-exposure.yaml` (CREATE, Silver) — to be generated by @doc-generator
- `governance/data-contracts/consumable-ai-exposure.yaml` (UPDATE, Gold, additive 4 fields, v1.0.0 -> v1.1.0 MINOR)

---

## Scope

This registry entry records the CDE flags introduced by the Anthropic Economic Index ingest. The PII scan verdict is **CLEAN** (`governance/pii/raw-anthropic-economic-index-pii-scan.md`); **zero new PII fields** are introduced at any zone. All CDE/PII decisions are fully justified in the primary tagging document — this file is the cross-referenced index so @governance-reviewer-post and @doc-generator can find the flags without re-reading the full narrative.

Existing CDEs on `consumable.ai_exposure` v1.0.0 (`soc_code`, `record_id`, `exposure_score`, `stat_res`, `boss_ai_score`, `rationale`, `task_breakdown_automatable`, `task_breakdown_human`, `scoring_model`, `model_tag`) are **not re-evaluated** by this spec per the no-propagation rule. They retain their v1.0.0 flags until S4 (`three-signal-ai-exposure-composite.md`) lands and replaces the single-source RES stat.

---

## New CDEs Introduced by This Spec

### Bronze — `raw.anthropic_economic_index` (6 CDEs)

| Column | Type | Business Term | Criticality | Rationale (one-liner — see tagging doc for detail) |
|--------|------|---------------|-------------|----------------------------------------------------|
| `task_id` | string | O*NET task ID (external standard) | P1 | Part of composite dedup grain [task_id, soc_code]; stable provenance key. |
| `soc_code` | string | BT-027 | P0 | Governed project-wide CDE. Composite dedup grain. FK to base.bls_ooh. |
| `task_pct` | double | Observed AI Exposure (per-task share, pending BT-NNN) | P0 | Per-(task, SOC) share; P0 SUM ~100 invariant; directly aggregated to Silver observed_exposure_pct. |
| `automation_pct` | double | Automation Share (Anthropic v2) | P0 | (directive + feedback_loop) * 100; drives MCP triple + Fight AI narrative. |
| `augmentation_pct` | double | Augmentation Share (Anthropic v2) | P0 | (task_iteration + validation + learning) * 100; co-equal to automation_pct under P1 invariant. |
| `source_release` | string | Release provenance (CC-BY 4.0) | P0 | License-critical attribution + reproducibility. |

### Silver — `base.anthropic_observed_exposure` (8 CDEs)

| Column | Type | Business Term | Criticality | Rationale |
|--------|------|---------------|-------------|-----------|
| `record_id` | string | — (grain hash) | P0 | Dedup grain. Idempotent re-run key. |
| `soc_code` | string | BT-027 | P0 | Primary key + sole JOIN key to Gold. |
| `observed_exposure_pct` | double | Observed AI Exposure (pending BT-NNN) | P0 | **The new signal.** SUM(task_pct) per SOC. S4 composite input. |
| `automation_pct` | double | Automation Share | P0 | Volume-weighted mean; MCP triple. |
| `augmentation_pct` | double | Augmentation Share | P0 | Volume-weighted mean; MCP triple. |
| `task_count` | int | Aggregation confidence | P1 | Range 1-141; low-evidence badge input. |
| `soc_match` | boolean | Gate flag | P0 | Governs Gold promotion; threshold re-baselined 80%->60% per EDA. |
| `source_release` | string | Release provenance | P0 | Aliased to anthropic_source_release in Gold. |

### Gold — `consumable.ai_exposure` additive (4 CDEs, all new)

| Column | Type | Business Term | Criticality | Rationale |
|--------|------|---------------|-------------|-----------|
| `observed_exposure_pct` | double | Observed AI Exposure | P0 | S4 composite empirical input; MCP triple; Fight AI boss detail. |
| `automation_pct` | double | Automation Share | P0 | MCP triple; "AI is doing the work" narrative. |
| `anthropic_task_count` | int | Aggregation confidence | P1 | S4 confidence weighting. |
| `anthropic_source_release` | string | CC-BY 4.0 attribution | P0 | MCP footer cite; S4 provenance block. |

---

## Totals

| Zone | Columns | CDE-Tagged | PII-Tagged | CDE Density |
|------|--------:|-----------:|-----------:|:-----------:|
| Bronze (Raw) | 12 | 6 | 0 | 50.0% |
| Silver (Base) | 10 | 8 | 0 | 80.0% |
| Gold (Consumable, additive) | 4 | 4 | 0 | 100.0% |
| **Total new tags** | **26** | **18** | **0** | 69.2% |

---

## Cross-References

- **Existing project-wide SOC CDE precedent:** `consumable.ai_exposure` v1.0.0 (`soc_code` flagged CDE, BT-027), `consumable.occupation_profiles`, `consumable.career_outcomes`, `base.onet_experience_profiles` (`bls_soc_code` CDE). This spec does not introduce `soc_code` as a new CDE — it re-affirms the existing governed classification on the Bronze + Silver tables this spec creates.
- **Sibling AI-exposure CDE pattern:** `consumable.ai_exposure` already tags `exposure_score`, `stat_res`, `boss_ai_score`, `rationale`, and the v4 Gemma additions (`task_breakdown_automatable`, `task_breakdown_human`, `scoring_model`, `model_tag`) as CDE. The 4 Anthropic additives continue that pattern — every new column on this Gold table has been added to serve a named downstream surface, so every new column is CDE.
- **Provenance CDE pattern:** `source_release` / `anthropic_source_release` tagged CDE at every zone matches the BEA RPP `data_year` precedent (`governance/cde-tagging/raw-ingest-bea-rpp.md`) where per-record vintage tagging rises to CDE because of reproducibility and license implications.

---

## Registry Hygiene

No global `cde-registry.md` index file exists in this project; the convention is one `*-cdes.md` file per spec in `governance/cde-registry/`. This file follows that convention and is discoverable by `ls governance/cde-registry/ | grep anthropic`. When @doc-generator runs, it should pick up:
1. This file (CDE counts + one-liner rationales)
2. The primary tagging doc at `governance/cde-tagging/raw-anthropic-economic-index.md` (full column-level rationale + YAML fragments ready to embed)

Both files must be read together; neither is a substitute for the other.
